# AGENTS.md — pointer

The entry file is `CLAUDE.md`. Read it first, then the folder's `CONTEXT.md` files. This file exists only so tools that load `AGENTS.md` find the repo rules. Added 2026-09-13 for agents outside Claude Code, with Hamoodey's approval.
